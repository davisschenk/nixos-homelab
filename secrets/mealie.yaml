# PLACEHOLDER — encrypt with: sops -e -i secrets/mealie.yaml before deploy
# Required keys:
mealie_oidc_client_secret: "REPLACE_WITH_AUTHENTIK_CLIENT_SECRET"
mealie_smtp_password: "REPLACE_WITH_SMTP_PASSWORD"
mealie_openai_api_key: "REPLACE_WITH_OPENAI_API_KEY"
