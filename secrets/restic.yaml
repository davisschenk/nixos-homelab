# PLACEHOLDER — encrypt with: sops -e -i secrets/restic.yaml before deploy
# restic_repository: path or URL, e.g. s3:s3.amazonaws.com/bucket or /data/backups/restic
# restic_environment: KEY=value pairs for backend auth (e.g. AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)
restic_repository: "REPLACE_WITH_REPOSITORY_URL"
restic_password: "REPLACE_WITH_STRONG_RANDOM_PASSWORD"
restic_environment: |
  REPLACE_KEY=REPLACE_VALUE
