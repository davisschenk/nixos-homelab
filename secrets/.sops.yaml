keys:
  - &admin age1ql3z7hjy54pw3hyww5ayyfg7zqgvc7w3j2elw8zmrj2kg5sfn9aqmcac8p  # admin workstation key: replace with real value (age-keygen -o ~/.config/sops/age/keys.txt)

  # HOST KEY — required for the host to decrypt secrets at boot.
  # After first boot, run:
  #   ssh-keyscan -t ed25519 mangrove | ssh-to-age
  # Paste the output below, uncomment, then run: just rekey
  # - &mangrove age1...

creation_rules:
  - path_regex: secrets/.*\.yaml$
    key_groups:
      - age:
          - *admin
          # - *mangrove  # uncomment after first boot + rekey
