version: 1
metadata:
  name: "Mangrove - VM Console"
  labels:
    blueprints.goauthentik.io/instantiate: "true"
entries:
  - model: authentik_providers_proxy.proxyprovider
    state: present
    identifiers:
      name: "VM Console Provider"
    attrs:
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      external_host: "https://console.schenkenberger.dev"
      mode: forward_single
      cookie_domain: "schenkenberger.dev"
  - model: authentik_core.application
    state: present
    identifiers:
      slug: "vm-console"
    attrs:
      name: "VM Console"
      slug: "vm-console"
      icon: "https://cdn.jsdelivr.net/gh/walkxcode/dashboard-icons@main/png/novnc.png"
      provider: !Find [authentik_providers_proxy.proxyprovider, [name, VM Console Provider]]
      policy_engine_mode: any
  - model: authentik_policies.policybinding
    state: present
    identifiers:
      target: !Find [authentik_core.application, [slug, vm-console]]
      group: !Find [authentik_core.group, [name, super-admin]]
      order: 0
    attrs:
      enabled: true
