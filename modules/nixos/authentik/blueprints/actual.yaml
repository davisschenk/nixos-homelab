version: 1
metadata:
  name: "Mangrove - Actual Budget OIDC"
  labels:
    blueprints.goauthentik.io/instantiate: "true"
entries:
  - model: authentik_providers_oauth2.oauth2provider
    state: present
    identifiers:
      name: "Actual Provider"
    attrs:
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      client_id: "actual"
      client_secret: !Env ACTUAL_OIDC_CLIENT_SECRET
      redirect_uris:
        - url: "https://actual.schenkenberger.dev/openid/callback"
          matching_mode: strict
      property_mappings:
        - !Find [authentik_core.propertymapping, [name, "authentik default OAuth Mapping: OpenID 'openid'"]]
        - !Find [authentik_core.propertymapping, [name, "authentik default OAuth Mapping: OpenID 'email'"]]
        - !Find [authentik_core.propertymapping, [name, "authentik default OAuth Mapping: OpenID 'profile'"]]
      sub_mode: hashed_user_id
      include_claims_in_id_token: true
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
  - model: authentik_core.application
    state: present
    identifiers:
      slug: "actual"
    attrs:
      name: "Actual Budget"
      slug: "actual"
      icon: "https://cdn.jsdelivr.net/gh/walkxcode/dashboard-icons@main/png/actual-budget.png"
      provider: !Find [authentik_providers_oauth2.oauth2provider, [name, Actual Provider]]
      policy_engine_mode: any
  - model: authentik_policies.policybinding
    state: present
    identifiers:
      target: !Find [authentik_core.application, [slug, actual]]
      group: !Find [authentik_core.group, [name, super-admin]]
      order: 0
    attrs:
      enabled: true
