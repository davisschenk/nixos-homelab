version: 1
metadata:
  name: "Mangrove - Grafana"
  labels:
    blueprints.goauthentik.io/instantiate: "true"
entries:
  - model: authentik_providers_oauth2.scopemapping
    state: present
    identifiers:
      name: "grafana OAuth Mapping: Groups"
    attrs:
      name: "grafana OAuth Mapping: Groups"
      scope_name: "groups"
      expression: |
        return {"groups": [group.name for group in request.user.ak_groups.all()]}
  - model: authentik_providers_oauth2.oauth2provider
    state: present
    identifiers:
      name: "Grafana Provider"
    attrs:
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      client_id: "grafana"
      client_secret: !Env GRAFANA_OIDC_CLIENT_SECRET
      redirect_uris:
        - url: "https://grafana.schenkenberger.dev/login/generic_oauth"
          matching_mode: strict
      property_mappings:
        - !Find [authentik_core.propertymapping, [name, "authentik default OAuth Mapping: OpenID 'openid'"]]
        - !Find [authentik_core.propertymapping, [name, "authentik default OAuth Mapping: OpenID 'email'"]]
        - !Find [authentik_core.propertymapping, [name, "authentik default OAuth Mapping: OpenID 'profile'"]]
        - !Find [authentik_providers_oauth2.scopemapping, [name, "grafana OAuth Mapping: Groups"]]
      sub_mode: hashed_user_id
      include_claims_in_id_token: true
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
  - model: authentik_core.application
    state: present
    identifiers:
      slug: "grafana"
    attrs:
      name: "Grafana"
      slug: "grafana"
      icon: "https://cdn.jsdelivr.net/gh/walkxcode/dashboard-icons@main/png/grafana.png"
      provider: !Find [authentik_providers_oauth2.oauth2provider, [name, Grafana Provider]]
      policy_engine_mode: any
  - model: authentik_policies.policybinding
    state: present
    identifiers:
      target: !Find [authentik_core.application, [slug, grafana]]
      group: !Find [authentik_core.group, [name, grafana-admin]]
      order: 0
    attrs:
      enabled: true
  - model: authentik_policies.policybinding
    state: present
    identifiers:
      target: !Find [authentik_core.application, [slug, grafana]]
      group: !Find [authentik_core.group, [name, grafana-viewer]]
      order: 10
    attrs:
      enabled: true
